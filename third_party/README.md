# Third-party lab definitions

`atomic-red-team/` is populated as a shallow sparse checkout and ignored by Git.
It is used only inside the isolated, authorized lab VM. No real malware should be
introduced into the scenario.

